apiVersion: v1
kind: Pod
metadata: 
  name: pod1
  labels:
    app: pod1
    type: frontend
spec:
  containers: 
  -  name: nginx-container
     image: nginx
  -  name: tomcat-container
     image: tomcat
  -  name: redis-container
     image: redis